define(['dojo/has', 'esri/config'], function(has, esriConfig) {
    window.SJCGIS = {
        topics: {
            search: {
                filter: 'f',
                reset: 'r',
                gather: 'g'
            }
        }
    };
    return window.SJCGIS;
});
